export const pong = (ping) => ({
    ...ping,
    PING: 'PONG',
    pid: process.pid,
});
export function ping(id) {
    return {
        ...(id && { id }),
        PING: 'PING',
        sent: performance.now(),
    };
}
export const isPong = (msg, ping) => !!msg &&
    typeof msg === 'object' &&
    !Array.isArray(msg) &&
    (ping === undefined
        ? typeof msg.id === 'string'
        : msg.id === ping.id) &&
    (ping === undefined
        ? typeof msg.sent === 'number'
        : msg.sent === ping.sent) &&
    msg.PING === 'PONG' &&
    typeof msg.pid === 'number' &&
    Object.keys(msg).length === 4;
export const isPing = (msg) => !!msg &&
    typeof msg === 'object' &&
    !Array.isArray(msg) &&
    typeof msg.id === 'string' &&
    msg.PING === 'PING' &&
    typeof msg.sent === 'number' &&
    Object.keys(msg).length === 3;
//# sourceMappingURL=ping.js.map