"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.report = exports.reportAll = void 0;
const typescript_1 = __importDefault(require("typescript"));
const get_canonical_filename_js_1 = require("./get-canonical-filename.js");
const cwd = process.cwd();
// Ignore the rootDir warning. Just means we're running something outside of
// cwd, which happens a lot in tests where we're spinning up the daemon and
// stuff repeatedly in test dirs, but almost never irl, and isn't a problem
// anyway.
const reportAll = (diagnostics, pretty) => diagnostics.filter(d => d.code !== 6059).map(d => (0, exports.report)(d, pretty));
exports.reportAll = reportAll;
const host = {
    getCurrentDirectory: () => cwd,
    getNewLine: () => '\n',
    getCanonicalFileName: get_canonical_filename_js_1.getCanonicalFileName,
};
const report = (diagnostic, pretty = !!process.stderr.isTTY) => pretty
    ? typescript_1.default.formatDiagnosticsWithColorAndContext([diagnostic], host)
    : typescript_1.default.formatDiagnostic(diagnostic, host);
exports.report = report;
//# sourceMappingURL=diagnostic.js.map