export const isDiagMode = (d) => d === 'ignore' || d === 'warn' || d === 'error';
export const getDiagMode = () => {
    const d = process.env.TSIMP_DIAG ?? 'warn';
    if (!isDiagMode(d))
        throw new Error(`Invalid TSIMP_DIAG environment variable: ${d}. Must be one of: ` +
            `'ignore', 'warn', or 'error'`);
    return d;
};
//# sourceMappingURL=diagnostic-mode.js.map